import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CapacityInfo = () => {
    return (
        <View>
            <Text>CapacityInfo</Text>
        </View>
    );
};

export default CapacityInfo;

const styles = StyleSheet.create({});
