import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ContractHistory = () => {
    return (
        <View>
            <Text>ContractHistory</Text>
        </View>
    );
};

export default ContractHistory;

const styles = StyleSheet.create({});
